import 'package:flutter/material.dart';

class ChatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF9FFE8),
      body: Center(
        child: Text("Chat với AgriVision AI", style: TextStyle(fontSize: 22)),
      ),
    );
  }
}
