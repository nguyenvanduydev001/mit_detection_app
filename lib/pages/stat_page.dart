import 'package:flutter/material.dart';

class StatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFFF9FFE8),
      body: Center(
        child: Text("Trang thống kê", style: TextStyle(fontSize: 22)),
      ),
    );
  }
}
